{{ if ne .chezmoi.os "linux" -}}
*/systemd

{{- else if ne .osid "linux-fedora" -}}
.var

{{- else if ne .osid "linux-debian" -}}
.bashrc

{{- else if ne .chezmoi.os "android" -}}
launch_keepassxc.sh

{{ end }}
